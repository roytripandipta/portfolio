# Personal Portfolio

Static portfolio site for _Your Name_, Principal Software Engineer.

- Built with plain HTML + CSS
- Deployed using GitHub Pages

## Run locally

Just open `index.html` in a browser.

## Deploy (GitHub Pages)

1. Commit and push to the `main` branch.
2. In **Settings → Pages**, set Source to `Deploy from a branch`, then select `main` and `/root`.
3. Wait a minute; your site will be live at `https://roytripandipta.github.io/portfolio`.
